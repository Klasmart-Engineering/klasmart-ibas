'use strict';

/**
 *  schedule-allocation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::schedule-allocation.schedule-allocation');
